# Lineage

What E-001's decision rule moved out of the build on 2026-09-09. Nothing here is
deleted from the record; it is kept where a reader can find it and a compiler cannot.

E-001's rule, fixed before the run: *"Waves lose if ΔR < 0 with the interval excluding
zero and ΔΦ's interval includes zero. The substrate becomes arm V. The chiral scale,
the callosum and `Ξ` move to a `docs/lineage/` record and out of the build."* The
result was ΔR = −0.333 [−0.333, −0.333] and ΔΦ = −0.001 [−0.001, +0.000]. See
[`../experiments/E-001-do-the-waves-earn-their-keep.md`](../experiments/E-001-do-the-waves-earn-their-keep.md) §Result.

| file | what it was | where its idea came from |
|---|---|---|
| `chiral_scale.rs` | the `10000.00001` number system as a type: bilateral positions, independent values, an asymmetry ratio, nine tests | ADR-0021 §Dimension Group Assignment; the archaeology's "chiral dimensional number system" |

The callosum and the bridge operator `Ξ = [R, G]` never reached code here; they were
waiting on E-001 and are recorded in ADR-0001 and the archaeology. E-002, which would
have measured them, is declined. E-003 is independent of the substrate and stands:
the operator's other reading, at the conscience boundary, is still to be measured.

A reader who wants the waves back has one experiment to beat: the same corpus, the
same probes, the same forgetting, and a recall@10 on zero-overlap-33 that is not a
third lower than a plain vector store's.
